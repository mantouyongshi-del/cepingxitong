# WonderKids 测评系统 · AI Agent 工作与协作规范 (AGENTS.md)

> 本文档为所有参与本项目的 AI Agent（开发、教研题库设计、架构设计、测试评估等）提供最高级别的项目背景、业务模型、架构规范、题库设计准则与协作工作流。

---

## 1. 项目定位与核心使命 (Project Mission & Vision)

**华儿街少儿探索中心 (WonderKids SteamClub) · 青少年计算思维与数字素养评估系统** 是专为 6~12 岁（小学 1-6 年级）少年儿童打造的沉浸式、自适应、多维度计算思维与数字能力诊断平台。

### 核心设计理论对标：
1. **中国《义务教育信息科技课程标准(2022年版)》**：深度对标信息意识、计算思维、数字化学习与创新、信息社会责任四大核心素养。
2. **国际 Bebras 计算思维挑战赛标准**：强调“无须前置编程语法，在趣味情境谜题中考查计算机科学底层思维”。
3. **皮亚杰儿童认知发展阶段理论**：针对 1-2 年级（具象感知）、3-4 年级（逻辑运筹）、5-6 年级（抽象架构）分层设计认知梯度。

---

## 2. 四维能力模型与 15 项子技能矩阵 (Competency Architecture)

系统采用科学的加权总分制与 15 项细分子能力下钻矩阵：

| 核心领域 | 权重 | 涵盖 15 项二级细分子技能 | 考查重点与目标 |
| :--- | :--- | :--- | :--- |
| **A. 计算思维能力** | **40%** | `分解` · `规律` · `抽象` · `算法` · `逻辑` | Bebras 经典五大支柱：问题拆解、模式规律识别、数据特征抽象、有限步算法推演与严密逻辑推理 |
| **B. 数字创造能力** | **30%** | `Scratch` · `Python` · `机器人` · `AI创作` | 综合创造与实践：图形化积木逻辑、代码语法与数据结构、智能硬件传感控制、AIGC 提示词协同创作 |
| **C. 数据与AI素养** | **20%** | `数据理解` · `AI认知` · `AI使用能力` | 智能时代素养：数据编码（二进制/点阵/哈希）、机器学习基础认知（训练集/分类/大模型）、AI 对话协同使用 |
| **D. 数字责任** | **10%** | `网络安全` · `隐私` · `AI伦理` | 信息时代安全合规：密码防护、网络隐私保护、防沉迷、版权与 AI 幻觉/偏见伦理意识 |

---

## 3. 技术栈与工程架构 (Tech Stack & Structure)

- **核心框架**：React 19 + TypeScript (~5.8) + Vite 6
- **样式与动画**：Tailwind CSS v4 + Lucide React 图标库 + Canvas Confetti 粒子系统 + Motion
- **可视化图谱**：Recharts 响应式四维雷达图与趋势矩阵
- **报表导出引擎**：`jspdf` + `html2canvas-pro`（已构建支持 3 页 A4 标准无损分页与全长截图引擎）
- **音效系统**：基于 HTML5 Web Audio API 的原声无依赖合成音效 (`src/utils/audio.ts`)
- **部署与端口**：默认绑定本地 `3000` 端口，公网支持 Cloudflare Tunnel (`cloudflared`)

### 核心目录结构：
```
src/
├── components/            # UI 组件层
│   ├── AssessmentView.tsx        # 答题界面（双栏免滚动控制台、线索示意、选项卡片）
│   ├── ProfileView.tsx           # 测评报告（雷达图、认知阶段、15项子技能、逐题归因）
│   ├── OfficialPdfReportModal.tsx# 官方 3 页 A4 PDF 报告模态窗与导出
│   ├── QuestionBankView.tsx      # 题库浏览器与题目录入管理
│   ├── DynamicClueIllustration.tsx # 40+ 种动态交互式题目动画线索图渲染引擎
│   └── LobbyView.tsx             # 探索大厅（年级营地选择、每日挑战宝箱）
├── data/                  # 题库与数据配置
│   ├── questions.ts              # 核心题库总表、领域元数据、算分引擎
│   ├── questions_grade1_2.ts     # 1-2年级（生活小侦探）题库
│   ├── questions_grade3_4.ts     # 3-4年级（编码探险家）题库
│   ├── questions_grade5_6.ts     # 5-6年级（算法架构师）题库
│   ├── medals.ts                 # 荣誉勋章馆配置
│   └── recommendations.ts        # 进阶课程与微课推荐库
├── utils/                 # 核心工具库
│   ├── questionGenerator.ts      # 智能抽题与选项混洗(Shuffle)引擎
│   ├── pdfExport.ts              # A4 多页 PDF 与长图高清导出引擎
│   └── audio.ts                  # Web Audio 交互音效合成器
└── types.ts               # 全局 TypeScript 类型定义
```

---

## 4. 题目形态硬性约束：生动的动态动画化情境呈现 (Mandatory Animated Presentation)

> ⚠️ **核心准则**：严禁使用“纯文字描述”或“死板的静态插图”呈现题目！针对 6~12 岁少儿，单调文字和静态图片具有极高的认知阅读门槛，严重违背儿童心理学规律。**每道题目必须以生动、直观、具有物理与算法过程流转的“动态动画（Animated Simulation）”形式呈现题干与核心线索！**

### 4.1 动态动画呈现的核心要求：
1. **动态物理与过程模拟 (Dynamic Physical & Process Simulation)**：
   - 题目线索区绝不是一张死图，必须是**运行中的动态微系统**；
   - 例如：
     - 水流与管道：水滴实时沿管道流动、阀门动态通断（`water-flow` / `dam-pipeline`）；
     - 仓储与物流：传送带实时滚动运送包裹、压栈出栈动态推移（`conveyor-stack` / `stack-queue`）；
     - 物理天平：称重时托盘伴随重力弹性倾斜摇摆（`sorting-scale`）；
     - 机器人寻路：智能小车伴随转向与路径轨迹动态移动（`robot-maze` / `drone-grid`）；
     - 二进制与点阵：信号灯按时序明暗闪烁、点阵扫描线实时推移（`binary` / `pixel-matrix` / `barcode-scan`）；
     - 逻辑门与电路：高低电平信号在导线中动态发光传导（`logic-gates`）；
     - 图论与网络：数据包在拓扑节点间沿边动态飞行与路由（`network-graph` / `mst-network`）；
     - 齿轮传动：主动轮与从动轮按齿比顺逆时针动态啮合转动（`gear-mesh`）。
2. **状态流转与分步演进 (State Transitions & Step Playback)**：
   - 动画需清晰表达算法的 `初始状态 ➔ 执行步骤 ➔ 目标/异常状态`，孩子只需观察动画演示，即可瞬间理解题干规则与挑战目标。
3. **视听协同与沉浸反馈 (Audiovisual Synergy)**：
   - 配合核心关键词高亮徽章（`highlightWords`）与一键语音朗读（`audioPrompt`），消除任何识字门槛。

### 4.2 分学段动画设计梯度：
- **1-2 年级（生活小侦探·具象感知）**：大色块、可爱角色、节奏明快舒缓的具象实体运动（如小动物采摘、积木拼搭、红绿灯变色、简易传送带）。
- **3-4 年级（编码探险家·逻辑运筹）**：操作流程与分支循环动画（如条件分支路口、分步循环计数、天平两两比对、迷宫探测雷达）。
- **5-6 年级（算法架构师·抽象架构）**：数据结构与计算模型动画（如拓扑排序依赖流、二叉树递归遍历、状态机跳转、哈希碰撞与动态规划表推演）。

### 4.3 测评系统真实性与严禁在答题进行状态剧透答案 (Assessment Integrity - No Spoilers)：
> ⚠️ **核心铁律**：本系统为专业的“青少年能力诊断测评系统”，而非题解查看器。在**答题进行状态（Active Assessment Mode）**中，绝不允许以任何形式直接或间接泄露题目的最终答案！
1. **动态动画禁透铁律**：
   - 动态仿真区旨在辅助孩子理解**“情境背景、实体规则、初始状态与探究机制”**，绝不允许直接在界面上自动计算出最终题目的答案（如直接输出最终权值求和数值、直接排列出最终完整序列等）；
   - 算力台或结算栏在题目挑战状态下必须呈现为未知待求问号（如 `左灯(4) + 中灯(2) = ❓`），引导孩子观察推导并在右侧自主作答。
2. **答题选项卡纯净无剧透**：
   - 答题过程中的选项卡片只呈现纯净具体的选项内容（如 `6个果子`），严禁渲染包含“正确答案/干扰项/计算解析”的辅助文案；
   - 题解剖析与错因分析只在**“交卷后的能力画像报告页”**以及**“题库管理后台/教师审查模态窗”**中呈现。

---

## 5. 题库设计与录入规范 (Question Authoring Guidelines)

所有 Agent 在设计或录入新题目时，必须严格遵守以下高标准规范：

### 题目必填属性与规范：
1. **情境化题干 (`storyContext` & `stemText`)**：
   - 必须设置趣味生活或科幻探索情境；
   - 题干精炼，必须包含 `highlightWords`（核心线索词高亮）。
2. **生动动画支持 (`clueType` & `clueBadgeText`)**：
   - 必须在 `DynamicClueIllustration` 匹配或扩展对应的 **动态动画组件**。
3. **选项科学设计 (`options`)**：
   - 选项 3~4 个，每个选项需有简明的 `description` 辅助解析；
   - 题库在运行期会自动混洗（Shuffle）选项，底层已实现完全均匀分布。
4. **题目解析与成因剖析 (`explanation` & `mistakeReason`)**：
   - `explanation`：通俗易懂的官方正解推导过程；
   - `mistakeReason`：剖析学生选错的典型思维误区（如“忽视了双向边连接”、“混淆了变量递增先后顺序”）。
5. **计算机科学底层原理映射 (`informaticsConcept`)**：
   - 每道题必须映射真实的 CS 原理（如 `title: '拓扑排序与依赖解析'`, `coreConcept: '...'`, `realWorldApplication: '软件包编译安装依赖、项目甘特图调度'`）。
6. **三级渐进式启发锦囊 (`stageHints`)**：
   - 必须提供 Level 1（审题要点）、Level 2（核心线索）、Level 3（解题关键步）三级提示，由浅入深引导孩子思考。

---

## 6. 打分机制与认知诊断算法 (Scoring & Diagnostic Engine)

1. **真实百分制加权（无保底分）**：
   - 严格根据答对题目的难度星级（★ 1.0x / ★★ 1.4x / ★★★ 1.8x）进行得分加权换算；
   - 全错严格输出 0 分，全对输出 100 分。
2. **同龄人击败率 (Percentile)**：
   - 依据正态分布与 Bebras 历史常模换算击败百分位。
3. **皮亚杰认知阶段定位**：
   - 结合综合得分、逻辑/算法/抽象维度的加权表现，智能判定学员当前处于 **具象感知期**、**逻辑运筹期** 或 **抽象架构期**。

---

## 7. Agent 协作与开发守则 (Rules for AI Agents)

1. **先验证后反馈**：每次修改代码后，必须执行 `npm run lint && npm run build` 确保 TypeScript 编译 0 报错；
2. **端口与服务守则**：项目标准端口为 `3000`，启动前必须检查端口占用，任务结束或休眠时需安全释放端口；
3. **多端响应式与免滚动体验**：答题界面保持右侧控制台紧凑布局，确保在不同屏幕尺寸下均无需无效纵向滚动；
4. **报告一致性**：UI 报告页 (`ProfileView.tsx`) 与 3 页 A4 官方认证 PDF 报告 (`OfficialPdfReportModal.tsx`) 的数据与指标必须 100% 保持同步与准确。
